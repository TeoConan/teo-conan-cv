stages:
  - build
  - deploy

# build dist
build:
  stage: build
  script:
    - echo "Running build in $(pwd)"
    - npm install
    - npm run build

deploy_develop:
  stage: deploy
  script:
    - echo "Project has been compiled"
  environment:
    name: develop
    url: https://dev.teoconan.fr